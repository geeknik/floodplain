# Floodplain runtime dependencies — exact-pinned for reproducible, auditable builds.
#
# Security note (AETHER doctrine §6 — supply-chain integrity):
#   * All direct dependencies are pinned to exact versions.
#   * For hash-verified installs use the generated lockfile: requirements.lock
#     (pip install --require-hashes -r requirements.lock).
#   * Re-run `pip-audit -r requirements.txt` after every bump.
#
# Phase 0 security bumps (2026-06): cryptography, pyOpenSSL and lxml moved past
# fixed-version caps that carried known CVEs; PyPDF2 (archived/EOL) replaced by pypdf.

adblockparser==0.7
dnspython==2.8.0
ExifRead==2.3.2
CherryPy==18.10.0
cherrypy-cors==1.7.0
Mako==1.3.12
beautifulsoup4==4.15.0
lxml==6.1.1
netaddr==0.10.1
pysocks==1.7.1
requests==2.34.2
ipwhois==1.1.0
ipaddr==2.2.0
phonenumbers==8.13.55
pygexf==0.2.2
pypdf==6.13.3
python-whois==0.7.3
secure==0.3.0
pyOpenSSL==26.3.0
python-docx==0.8.11
python-pptx==0.6.23
networkx==2.6.3
cryptography==49.0.0
publicsuffixlist==0.9.4
openpyxl==3.1.5
pyyaml==6.0.3
